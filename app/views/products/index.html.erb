<section id="content-details" class="row clearfix">
  <article id="details-article" class="col-sm-8">
    <h1 id="article-title">Comic plans</h1>
  </article>
</section>
<section id="products">
<% @products.each do |product| %>
 <div class="product clearfix">
   <a href="<%= product_path(product) %>">
    <div class="product-image-container">
     <img id="comics" src="<%= asset_path product.image_name %>" id="comics"  alt="<%= product.title %>"/>
     <span class="product-price">$<%= product.price %></span>
   </div>
   <p class="product-title"><%= product.title %></p></a>
 </div>
 <% end %>
</section>